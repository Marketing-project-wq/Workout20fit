tools/__pycache__/
__pycache__/
*.pyc
node_modules/

# Secrets — never commit the YouTube API key.
.env
.env.*

# Re-validation job output (TUGAS 6).
logs/

# keep the example env (real .env stays ignored above)
!.env.example
